#ifndef INTERPRETER_H
#define INTERPRETER_H

// The interpret function takes a filename as an argument, parses the file into an Abstract Syntax Tree (AST), evaluates the AST, and returns the result.
int interpret(const char* filename);

#endif // INTERPRETER_H